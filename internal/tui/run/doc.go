// Package tui is the composition root for the TUI: it wires the chat
// session, the adapter, and the view into one Bubble Tea program.
//
// It may import internal/cli and the runtime packages, plus
// internal/tui/adapter and internal/tui/view. cmd/mivia is its only caller.
package tui
