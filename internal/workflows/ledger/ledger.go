// Package ledger provides durable persistence for workflow runs.
// It implements the workflow repository contract with SQLite projections
// following the established storage migration pattern.
//
// Phase 2 deliverable — ledger, isolated worktree, and lifecycle.
package ledger
