// Package orchestrate implements the dispatch_tasks and join_run
// orchestration tools: task routing, wave dispatch, timeouts, diagnostics,
// and run lifecycle.
//
// It may import the runtime orchestration packages below internal/cli.
// The internal/cli and internal/cli/chat packages may import it.
package orchestrate
