{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["findings", "finding_count", "no_findings", "has_perf", "inspected"],
  "properties": {
    "findings": {
      "type": "array",
      "maxItems": 2,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "class", "severity", "title", "invariant", "evidence", "reachable_path", "impact", "remediation", "regression_test", "sweep"],
        "properties": {
          "id": { "type": "string", "minLength": 1, "maxLength": 64 },
          "class": { "type": "string", "enum": ["perf", "logic"] },
          "severity": { "type": "string", "enum": ["low", "medium", "high", "critical"] },
          "title": { "type": "string", "minLength": 1, "maxLength": 120 },
          "invariant": { "type": "string", "minLength": 1, "maxLength": 600 },
          "evidence": { "type": "string", "minLength": 1, "maxLength": 1000 },
          "reachable_path": { "type": "string", "minLength": 1, "maxLength": 600 },
          "impact": { "type": "string", "minLength": 1, "maxLength": 500 },
          "remediation": { "type": "string", "minLength": 1, "maxLength": 500 },
          "regression_test": { "type": "string", "minLength": 1, "maxLength": 300 },
          "sweep": { "type": "string", "minLength": 1, "maxLength": 2000 }
        }
      }
    },
    "finding_count": { "type": "integer", "minimum": 0, "maximum": 2 },
    "no_findings": { "type": "boolean" },
    "has_perf": { "type": "string", "enum": ["true", "false"] },
    "inspected": {
      "type": "array",
      "minItems": 1,
      "maxItems": 40,
      "items": { "type": "string", "minLength": 1, "maxLength": 200 }
    }
  }
}
