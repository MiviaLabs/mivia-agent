# System Invariants

These are **non-negotiable** properties of the mivia agent that any change must preserve.
Before committing changes that touch the relevant area, run the corresponding invariant
test(s) and confirm they pass.

## Categories

- **Safety** - property that can be verified in a unit test on a finite state set.
  A failing test proves the invariant is violated.
- **Liveness** - property that asserts something *eventually* happens. Unit tests
  cannot fully prove liveness; they can only verify partial behavior. Treat liveness
  invariants as requiring integration/stress tests or accepted residual risk.

## TUI / Rendering

| ID | Category | Invariant | Test(s) | Last Verified |
|----|----------|-----------|---------|---------------|
| INV-TUI-1 | Safety | Bridge drain is the exclusive runtime content source of truth for the TUI | `TestBridgePathAssistantToolsAndFinish` | |
| INV-TUI-2 | Liveness | pollCmd always re-queues itself regardless of data availability (no chain death) | `TestTuiTickMsgAlwaysRequeuesPoll`, `TestPollCmdUsesBridgeNotAdapterOnly`, `TestTuiTickMsgStressRequeuesPoll`, `TestTuiTickMsgStressWithBridgeData` | |
| INV-TUI-3 | Safety | finishStream is idempotent - calling it twice does not produce duplicate blocks | `TestFinishStreamIdempotent`, `TestBridgeDrainNotDoubleProcessed` | |
| INV-TUI-4 | Safety | uiEventMsg always re-queues pollCmd in chat mode | `TestUIEventMsgStepUpdatesDetail`, `TestUIEventMsgErrorSetsStalled` | |
| INV-TUI-5 | Safety | Smoke journey end-to-end completes without panic | `TestTUISmoke_FullJourney` | |
| INV-TUI-6 | Liveness | Tool progress events are visible in TUI during parallel execution (tools don't look hung) | `TestStreamBridgeQueuedRunningDoesNotDoubleCountActiveTools`, `TestStreamBridgeConcurrentDispatchCompleteness`, `TestStreamBridgeConcurrentDispatchAndTUIApply`, `TestBridgeConcurrentWriteAndDrainRace`, `TestBridgeConcurrentFinishAndDrainRace`, `TestStreamBridgeConcurrentActiveToolsNoDeadlock` | |
| INV-TUI-7 | Safety | Empty-Content tool turns get honest status (never blank); no fake assistant speech. Status lines redact secrets **when a redaction policy is configured** - never by a compiled-in pattern | `TestEmptyContentToolsGetStatusLine`, `TestShortInterimRejectedUsesStatus`, `TestToolStatusLine_RedactsSecrets` || 2026-07-30 (plan 10 implemented; no compiled patterns remain) |
| INV-TUI-8 | Safety | Interim quality gate rejects ghost bubbles; real prose still commits | `TestInterimRejectedWhenTooShort`, `TestInterimAcceptedWhenRealProse`, `TestPushInterimGatesGhosts`, `TestInterimAssistantBecomesChatBubble` | |
| INV-TUI-9 | Safety | A turn that ends before completing preserves its partial story (interim/tools/answer) plus a cancelled footer. Scope is any turn that stops early, not only Ctrl+C: an empty-Enter force-send reaches `startAI` while `m.waiting`, and `startAI` resets `streamBuf`/`thinkingBuf`/`toolRows` and swaps the bridge, so the superseded turn must be committed first or its visible answer is destroyed and two user blocks land back to back | `TestCancelKeepsInterimAndToolsInHistory`, `TestCancelBeforeFirstActivity`, `TestCancelKeepsPartialAnswerOnForceSend` | 2026-07-30 (widened to superseded turns) |
| INV-TUI-10 | Safety | Scroll follow helper + awaiting planning affordance | `TestShouldFollowOutput`, `TestAwaitingFirstActivityPlanning` | |
| INV-TUI-11 | Safety | Follow mode preserves YOffset on content growth; jump-to-latest restores bottom | `TestFollowPreservesOffsetWhenContentGrows`, `TestNoteUserScrolledUpThenPollDoesNotYank`, `TestJumpToLatestKeyPath` | |
| INV-TUI-12 | Safety | Cancel then bus TurnEnd does not duplicate cancelled footer | `TestCancelThenTurnEndDoesNotDuplicateFooter` | |
| INV-TUI-13 | Safety | View-only hydrate reconstructs empty-speech status; pure hydrate unchanged | `TestReconstructStatus_EmptyContentTools`, `TestReconstructStatus_DoesNotMutateMessages` | |
| INV-TUI-14 | Safety | Classic (`--plain`) interim gates + no final double-print, and a message boundary is real. `MarkdownWriter` is line buffered and emits only on a newline, so an assistant message that does not end in one is held until a later message supplies it - two messages then render as one run-on paragraph. Every revocation therefore terminates the message. The two classic surfaces differ deliberately: the REPL streams live to the terminal, where revoked speech cannot be withdrawn and the boundary is all that can be fixed, while one-shot `-p` collects into a buffer printed once, where a revocation withholds narration entirely so stdout carries the answer and stderr the progress log | `TestClassicUI_InterimPrintedWhenNoStreamBytes`, `TestClassicUI_InterimSkippedWhenAlreadyStreamed`, `TestClassicUI_FinalEventNotPrinted`, `TestClassicUI_BufferedKeepsInterimOutOfFinalOutput`, `TestClassicUI_BufferedRevokeLetsInterimPrint`, `TestClassicUI_LiveMessagesDoNotGlue` | 2026-07-30 (message boundaries) |
| INV-TUI-15 | Safety | History work groups collapse dense tools; final assistant outside | `TestWorkGroupAutoCollapseAt4`, `TestWorkGroupFinalAssistantOutside`, `TestFindWorkGroups_SplitsOnInterim` | |
| INV-TUI-16 | Safety | Model-level scroll acceptance (Update paths). The transcript viewport consumes keys only while it owns focus: bubbles' `viewport.DefaultKeyMap` binds bare runes (`u`/`d`/`b`/`f`/space/`k`/`j`/`h`/`l`) and the arrow keys, and the viewport has no focus concept, so without a gate typing in the composer scrolls history and latches `followOutput` off for the rest of the session. The chat run dashboard is held to the same rule - it renders above the composer and consumes keys before it, so it binds non-typable keys only, and it consumes them only while actually drawn (`isVisible`, not `open`) **and while the transcript side owns focus**: a dashboard toggled open with no runs draws nothing, and letting that state swallow up/down left the arrow keys dead for both panes with nothing on screen to explain it, while a *visible* dashboard that owned the arrows regardless of focus stole the caret from anyone editing a multi-line draft. Scope is chat mode. The welcome-screen session picker deliberately still binds `k`/`j` on an empty composer, pinned by `TestWelcomeJKNavOnlyWhenComposerEmpty` - it is pre-existing and outside this row | `TestScrollAccept_MouseWheelUpUnfollowsAndStreamDoesNotYank`, `TestScrollAccept_EndKeyJumpToLatestViaUpdate`, `TestScrollAccept_ConcurrentTicksWhileScrolledUp`, `TestScrollAccept_TypingInComposerDoesNotScrollOrUnfollow`, `TestScrollAccept_ComposerCursorKeysDoNotScrollTranscript`, `TestScrollAccept_ViewportGateFollowsFocus`, `TestScrollAccept_ScrollbackKeysStillScroll`, `TestScrollAccept_EmptyDashboardDoesNotSwallowArrowKeys`, `TestScrollAccept_VisibleDashboardOwnsArrowKeys`, `TestScrollAccept_VisibleDashboardLeavesComposerArrows` | 2026-07-30 (focus gate added) |
| INV-TUI-17 | Liveness | tea.Program event-loop scroll (Send + live pollCmd) preserves follow/YOffset | `TestScrollProg_WheelUpUnfollow_PollDoesNotYank`, `TestScrollProg_EndKeyJumpToLatest`, `TestScrollProg_ConcurrentPollWhileScrolledUp` | |
| INV-TUI-18 | Liveness | Linux PTY CSI keys drive scroll follow (End/PgUp) under tea.Program | `TestScrollPTY_EndKeyViaBytes`, `TestScrollPTY_PgUpViaBytesUnfollows` | |
| INV-TUI-19 | Safety | Mouse auto-enables when available; MIVIA_MOUSE override | `TestMouseAvailable_EnvOverride`, `TestMouseAvailable_DumbTERM`, `TestNewTUIModel_MouseFollowsAvailability` | |
| INV-TUI-20 | Liveness | Linux PTY SGR mouse wheel unfollow/refollow | `TestScrollPTY_CSIMouseWheelUnfollows`, `TestScrollPTY_CSIMouseWheelDownRefollows` | |
| INV-TUI-21 | Safety | Paint frame shows latest when following; glyph chrome bounded | `TestScrollProg_PaintFollowShowsLatestMarker`, `TestScrollIndicator_GlyphWidthBounded` | |
| INV-TUI-22 | Safety | Raster cell-grid paint timing: marker in cols×rows bitmap within budget | `TestPaintRaster_TimingBudgetToMarker`, `TestPaintRaster_UnfollowFrameDoesNotExceedCellBudget`, `TestPaintRaster_UnitRasterize` | |
| INV-TUI-23 | Safety | Every key affordance the UI advertises is really bound, and no key does two jobs at once. `/help`, the tool-panel hint and the run-dashboard footer may not name a key no handler implements, and a removed binding must be inert rather than half-working: `ctrl+d` no longer quits (it sat beside a scroll key, so reaching one key over exited the app), the readline editing keys `ctrl+u`/`ctrl+k`/`ctrl+w` perform their edits in the composer while the transcript viewport's keymap strips its `ctrl+u`/`ctrl+d` scroll aliases (a destructive editing key must never double as a scroll key in another pane - they were once swallowed entirely to mask that dual meaning), `ctrl+←`/`ctrl+→` move by words (bubbles binds only the Emacs alt-forms; real terminals send CSI 1;5D/C), the composer textarea and transcript viewport are built exclusively by `newComposerTextarea`/`newTranscriptViewport` so the declared keymaps cannot be silently lost on the resize path, and `ctrl+m` is bound to nothing because 0x0D *is* carriage return (bubbletea aliases KeyCtrlM to KeyEnter), so a "ctrl+m" branch is dead code from a real terminal while the chord actually sends the draft. The TUI `/help` dialog renders `tuiHelpContent` and is asserted against its rendered lines; the classic REPL's `replHelpContent` documents different keys and the two surfaces must never share a help source (the TUI once displayed the REPL's "Ctrl+U kill line"/"Ctrl+D exit"/"Tab completion" while its honesty test checked a string nothing rendered). Keys the UI does claim must work: `home`/`end` are the composer's line-start/line-end keys while it has focus and scroll the transcript to the oldest/latest message while the transcript has focus (`end` on an empty draft still jumps to latest, so the advertised "↓ latest" affordance needs no focus cycle; `shift+home`/`shift+end` reach the transcript from the composer where the terminal forwards them - VTE and Konsole bind both to their own scrollback), focusing the tool strip moves focus off the composer so its advertised expand is reachable, and a run held by another process renders as held. The composer border is one fixed colour with no phase glow and no focus flip - printable input returns focus to the composer immediately, so a missing blur cue loses no keystroke | `TestScrollAccept_EditingKeysEditTheDraft`, `TestScrollAccept_DestructiveKeysNeverScroll`, `TestScrollAccept_CtrlArrowWordMotion`, `TestScrollAccept_CtrlDDoesNotQuit`, `TestWelcomeCtrlDDoesNotQuit`, `TestScrollAccept_HomeGoesToTopWhenScrollbackFocused`, `TestScrollAccept_HomeMovesComposerCursor`, `TestScrollAccept_EndMovesComposerCursor`, `TestScrollAccept_EndOnEmptyDraftJumpsToLatest`, `TestScrollAccept_ShiftHomeEndReachTranscriptFromComposer`, `TestScrollAccept_ComposerNoFocusColorWhileWaiting`, `TestToolPanelFocusEnablesExpand`, `TestToolPanelHintOnlyClaimsWorkingKeys`, `TestSlashHelpMatchesRealBindings`, `TestCtrlMDoesNotToggleMouse`, `TestRunDashboardRendersHeldRuns` | 2026-08-03 (composer chrome: fixed border colour, no blur text, model label on border; TUI help source split from REPL; ctrl+m dead binding removed) |
| INV-TUI-24 | Safety | Pasted input is never silently dropped, and a failed paste is never silent. A bracketed paste arrives as one `KeyMsg` with `Paste=true`; it is inserted atomically, never routed as keys (its newlines must not reach the Enter handler and fire N sends), and a payload larger than bubbletea's 256-byte read buffer must reassemble rather than truncate. With the transcript focused the composer is blurred and bubbles' textarea drops every key, while `routeFocusKey`'s printable-input refocus cannot fire for a paste (bubbletea wraps pasted runes in `[...]`, so the key string is multi-rune) - so paste refocuses the composer instead of vanishing. Modal surfaces still swallow it: text landing in a composer hidden behind a dialog is worse than nothing. `ctrl+v` is read by mivia's own tool list (`clipboardReadTools`, Wayland included) rather than bubbles' atotto binding, which has no Wayland reader and reports failure into `textarea.Err` - a field nothing renders | `TestPasteWhileScrollbackFocusedReachesComposer`, `TestPasteAppendsToExistingDraft`, `TestMultilinePasteDoesNotSend`, `TestPasteStaysSwallowedWhileModalOpen`, `TestPasteInWelcomeModeInserts`, `TestClipboardReadCommandPrefersAvailableTool`, `TestClipboardReadReportsFailureWhenNoToolExists`, `TestCtrlVFailureIsVisible`, `TestScrollPTY_BracketedPasteInsertsWithoutSending`, `TestScrollPTY_LargePasteIsNotTruncated`, `TestScrollPTY_PasteWhileScrollbackFocusedIsNotLost` | 2026-07-31 |
| INV-TUI-25 | Safety | A copy reports what actually happened, where the user can see it. Delivery returns `copyResultMsg`: `cmd.Run()`'s error was previously discarded, so a `wl-copy` with no compositor still printed "copied N chars" and the user pasted stale content elsewhere. Transient acknowledgements live in their own `notice`/`noticeAt` field, never in `stepDetail`: sharing the field meant a copy mid-turn replaced the live tool heartbeat with a string that never expired on the footer or status bar, and silently reset the stall clock. Delivery walks **every** installed writer until one succeeds - stopping at the first binary *found* reported "no clipboard tool reachable" on an X11 box where `wl-copy` failed and `xclip` was never tried. The acknowledgement renders while **idle** - the status bar shows `stepDetail` only during a turn, which is exactly when nobody is copying - and expires after `copyNoticeTTL` so a stale notice never reads as a claim about the present. The OSC 52 size limit belongs to that transport alone: a local clipboard binary copies text of any size, so an oversized block is refused only when no binary exists. OSC 52 is written to `/dev/tty` (overridable by `MIVIA_CLIPBOARD_TTY` in tests), not stdout, so it reaches the terminal under redirection and does not leak into captured output; the interleave with bubbletea's renderer goroutine is a known accepted race bounded by a single Write | `TestCopyAckVisibleWhenIdle`, `TestCopyAckExpires`, `TestCopyAckReportsToolFailure`, `TestCopyLargeTextUsesLocalToolBeyondOSC52Limit`, `TestCopyRightClickAckVisible`, `TestYankKeyCopiesSelectedBlock`, `TestCtrlCCopiesOnlyWhenIdleWithSelection`, `TestRightClickCopiesBlock`, `TestOSC52Encoding`, `TestCopyFallsBackToNextInstalledTool`, `TestCopyDuringTurnKeepsHeartbeat` | 2026-07-31 |
| INV-TUI-26 | Safety | Idle `ctrl+c` is never an unguarded exit, and select mode never occupies an editing key. While a turn runs `ctrl+c` cancels and nothing else (staged cancel untouched). At rest the precedence is: copy a selected message (in **either** focus - requiring scrollback focus meant the reflexive press quit the app instead, and the copy consumes the selection so a later press can still reach quit) · else clear a non-empty draft and arm · else arm. A second press within `quitArmWindow` quits; the arm expires and **any** other key disarms it, or a `ctrl+c` minutes later exits with no warning. **Every** kind of user input disarms it, not only keys - the disarm lives on the `Update` paths for mouse and paste as well, because `handleChatKey` is never reached for either, and an arm that survived a click restored the exact foot-gun it was added to remove. `ctrl+c` and `ctrl+q` outrank every modal surface: the UI invites `ctrl+g` mid-turn, and a dialog that swallowed them left no way to stop a runaway turn. The welcome screen keeps a plain `ctrl+c` quit (no draft worth protecting, no selection to copy) and honours `ctrl+q` like every other screen. Select mode is `F2` or `/select`, never `ctrl+e`: bubbles binds that to line-end, and with `home`/`end` restored to the composer it was the last key between the user and the end of their own line. Chrome that names the select-mode key must change with it | `TestCtrlCCopiesSelectionFromComposerFocus`, `TestCtrlCClearsDraftBeforeQuitting`, `TestCtrlCArmsThenQuits`, `TestCtrlCArmExpires`, `TestCtrlCArmClearedByOtherInput`, `TestCtrlCDuringTurnStillCancels`, `TestSelectModeOnF2`, `TestCtrlEIsLineEndNotSelectMode`, `TestSelectSlashCommandTogglesMode`, `TestWelcomeCtrlQQuits`, `TestSelectModeReleasesTheMouse`, `TestSelectModeKeyAvoidsFlowControl`, `TestIntegration_QuitAfterCancel_DoesNotStrand`, `TestCtrlCArmClearedByPaste`, `TestCtrlCArmClearedByMouseSelection`, `TestCtrlCCancelsWithFleetOverlayOpen`, `TestCtrlQQuitsFromEveryModalSurface`, `TestArmNoticeDisappearsWithTheArm`, `TestEndMovesCaretWithWhitespaceOnlyDraft` | 2026-07-31 |
| INV-TUI-27 | Safety | Key documentation is generated, never hand-maintained. `keyRegistry` (keymap.go) declares every binding with its scope, and `/help` renders its key sections from those rows - a binding cannot be documented without being declared, which is the drift that let `/help` advertise the classic REPL's keys while its honesty test asserted on a string nothing rendered. `validateKeyRegistry` rejects a key bound twice in one scope, a row with no help, and any `forbiddenKeys` entry: `ctrl+m`/`ctrl+i`/`ctrl+j` are carriage return/tab/newline at the byte level (bubbletea reports them as enter/tab, so a branch on those strings is unreachable from a real terminal while passing tests), and `ctrl+s` is XOFF. Registered global chat keys must actually be consumed by the router, so a documented row that nothing handles fails the build. Bracketed paste is deliberately not a registry row - it arrives as a message flag, not a key string, and is routed before key dispatch. The check runs in **both** directions: a registered key the router ignores fails, and a behavioural probe drives a candidate key universe through every surface so a handler acting on a key nobody declared fails too - that is the direction that produces undocumented keys. Slash commands that change input state (`/select`) apply it in the handler and stage only the terminal command, which every caller of `handleSlash` drains; staging the state change itself made `/select` a no-op from the queue and the welcome screen and then fired it later on an unrelated command. Scope precedence is stated once, in keymap.go's header, in the order the router applies it | `TestKeyRegistryIsStructurallySound`, `TestKeyRegistryRejectsForbiddenAliases`, `TestKeyRegistryRejectsDuplicateInScope`, `TestHelpIsGeneratedFromRegistry`, `TestRegisteredChatKeysAreReallyBound`, `TestForbiddenKeysAreInertInTheRouter`, `TestSlashHelpMatchesRealBindings`, `TestEveryBoundKeyIsRegistered`, `TestQueuedSelectTogglesImmediately`, `TestWelcomeSelectTogglesImmediately` | 2026-07-31 |

## Agent Loop

| ID | Category | Invariant | Test(s) | Last Verified |
|----|----------|-----------|---------|---------------|
| INV-AG-1 | Safety | Tool surface Description() and schema JSON validate against OpenAI schema | `TestSearchOpenAISchema` | |
| INV-AG-2 | Safety | run_command presents as last-resort (argv, not shell) | `TestToolSurfacePreferFilesystemOverRunCommand` | |
| INV-AG-3 | Safety | Session.SendUser is not data-racy on Messages field | `TestSessionMessagesConcurrentReadWrite`, `TestSessionMessagesConcurrentLoadMore`, `TestSessionMessagesRaceDetector` | |
| INV-AG-4 | Safety | Multi-step subagent gets tool access; one-shot does not | `TestDelegateToolMultiStepTrue`, `TestDelegateToolMultiStepFalse` | |
| INV-AG-5 | Safety | Tool argument redaction is opt-in, default shows args. Redaction patterns are configuration only - no pattern is compiled in, so an unconfigured workspace redacts nothing (plan 10) | `TestRedactToolInputDefaultShowsArgs` || 2026-07-30 (plan 10 implemented; no compiled patterns remain) |
| INV-AG-6 | Safety | Multi-step subagent uses tools when handler is multi_step | `TestMultiStepHandlerToolAccess` | |
| INV-AG-7 | Safety | Nested agents execute only tools in their restricted registry; scoping is enforced at the dispatcher, not advertised | `TestMultiStepHandlerCannotExecutePrivilegedToolThroughParentDispatcher`, `TestMultiStepHandlerScopedDispatcherIsNotParent`, `TestLoopRejectsDispatcherToolMissingFromVisibleRegistry` | |
| INV-AG-8 | Safety | Text the model produced is never lost silently. A turn that produced no assistant text anywhere fails loudly on an interactive surface instead of returning an empty success that renders as a bare "done" - and deliberately does NOT fail for sub-agents, because `buildResult` discards a task's output whenever its error is non-nil, so a task that did its work through tools and stopped without prose has still succeeded. Emptiness is decided by a `TrimSpace` **predicate** while stored and streamed bytes stay verbatim, so an answer opening with an indented code block keeps its indentation. An answer delivered by the provider's internal non-stream retry still reaches `StreamWriter`, since the caller's `stream` flag stays true and it will not rewrite the answer itself. An interrupted turn (cancel or deadline) keeps in history exactly the text already streamed to the screen, and is persisted; a truncated or upstream-errored stream keeps nothing, because a fragment replayed as a completed turn is what the provider's completion-signal guard exists to prevent | `TestNoMessageLossEmptyTurnFailsLoudly`, `TestNoMessageLossEmptyTurnStaysSuccessfulForSubAgents`, `TestNoMessageLossPreservesLeadingWhitespace`, `TestNoMessageLossInterruptedTurnKeepsWhatWasShown`, `TestNoMessageLossTruncatedTurnDoesNotPoisonHistory`, `TestNoMessageLossInterruptedTurnIsPersisted`, `TestNoMessageLossFallbackWritesToStreamWriter`, `TestNoMessageLossFallbackToleratesNilWriter`, `TestLoopFallsBackToLastTextWhenFinalResponseEmpty`, `TestLoopFallsBackToLastTextWhenFinalToolCallOnly`, `TestMultiStepHandlerEmptyFinalAfterToolStaysCompleted`, `TestCompletedPlainLegacyTurnReportsAutosaveFailure`, `TestInterruptedPlainLegacyTurnReportsAutosaveFailure`, `TestOneShotPrintsReplyBeforeAutosaveFailure`, `TestOneShotPrintsPartialBeforeCancellation`, `TestOneShotHidesOrdinaryFailedStream` | 2026-08-10 |
| INV-AG-9 | Safety | Orchestration run handles are accessible only to the creator's session principal; unauthorized and unknown run IDs are indistinguishable. Read-only run tools are in scope: `list_run_events` gates on the same principal. Run creation is in scope too: a `spawn_agent` idempotency key resolves only within its creator principal, so a foreign key behaves as a first use rather than returning another principal's run or a conflict. Content resolution is deliberately outside this invariant: a reference is not a run id, it survives its run handle's retention window, and gating it on a handle would stop resolving a reference the model still holds - see INV-AG-12 and INV-AG-16 | `TestRunHandleNotAccessibleToOtherOwner`, `TestRunHandleAccessibleToAncestor`, `TestCancelRunCannotCancelForeignRun`, `TestUnauthorizedAndUnknownAreIndistinguishable`, `TestListRunEventsRequiresRunOwnership`, `TestSpawnForeignPrincipalIsIndistinguishableFromFirstUse` | 2026-07-30 (creation scope added, plan 22) |
| INV-AG-10 | Safety | A content reference handed to the model resolves, or it is not handed to the model. Minting lives in exactly one function (`contentref.Reference`); only content the coordinator actually persisted gets a reference, so a failed content write leaves none and layers with no repository (the dispatcher's failure envelope, the multi-step handler, run-level errors) mint none at all; model-visible references are read from the task record rather than re-minted; a malformed reference is reported distinctly from absent content, so `not_found` stays evidence | `TestModelVisibleOutputRefResolves`, `TestModelVisibleErrorRefResolves`, `TestReferenceHasSingleMinter`, `TestStoreContentFailureBlocksRef`, `TestModelVisibleRefsOmittedWhenContentWriteFailed`, `TestDispatcherFailureOmitsUnstoredRefs`, `TestDispatchTasksErrorEnvelopeOmitsUnstoredReference`, `TestSpawnResultPayloadRecoveredRunUsesStoredRefs`, `TestLedgerReadRejectsMalformedRef` | 2026-07-30 (plan 19 implemented) |
| INV-AG-11 | Safety | Recorded execution history survives a projection rebuild: a replayed lifecycle event keeps its kind, task, attempt and original `created_at`, so a kind filter cannot silently return zero rows and a timestamp is when the event happened, not when it was read back. The projection stamps only what arrives unstamped, at run level as well as event level, so `mivia diagnostics` reports a recovered run's real age. Event order is taken from `sequence`, never from a timestamp, so tied or clock-inverted timestamps cannot scramble it; `sequence` itself stays derived from store order. Events recorded before plan 21 hold no durable timestamp and still replay to the read instant, and a replayed event still reports the storage row id rather than the caller's | `TestListEventsRestoresKindAfterProjectionRebuild`, `TestListEventsToleratesUndecodablePayload`, `TestListEventsPreserveOriginalTimestampAcrossRebuild`, `TestAppendEventStampsBeforeMarshalling`, `TestListEventsOrderedBySequenceUnderTiedTimestamps`, `TestLegacyRowWithoutTimestampFallsBackToReadInstant`, `TestMemoryCreateRunPreservesSuppliedCreatedAt`, `TestMemoryCreateRunStampsWhenUnstamped`, `TestMemoryAppendEventStampsOnlyUnstampedEvents`, `TestProjectionStateIncludesTimestampsAcrossRebuild` | 2026-07-30 (plan 21 implemented) |
| INV-AG-12 | Safety | Content resolution is deliberately NOT principal-scoped: `ledger_read` resolves any well-formed reference any caller presents, so the content digest is an equality oracle over everything the store holds, and content outlives its run - `DeleteRun` removes a run's events but deliberately removes no content (INV-AG-14), and there is no retention. Accepted, not overlooked: one principal exists per process, a sub-agent shares its parent's principal by design, and a process-local admission set would stop resolving a reference the model still holds across a restart while defending against no principal that exists. So the only two answers `ledger_read` gives about a reference are about the bytes - malformed shape, or absent content - and `not_found` keeps meaning the bytes are absent. Severity analysis and the rejected alternatives are in plan 20 §1c and corrections C2/C3/C7 | `TestLedgerReadWorksOnMemoryBackend`, `TestLedgerReadRejectsMalformedRef`, `TestLedgerReadReportsNotFoundForAbsentContent`, `TestLedgerToolsAreUnprivilegedAndReachSubAgents`, `TestListRunEventsRequiresRunOwnership` | 2026-07-30 (plan 20 validated → accept and document) |
| INV-AG-13 | Safety | A run is executed by at most one process at a time. A second process on the same store cannot resume or spawn a run while a live executor holds its lease. The executor refreshes the lease during long task execution. A new executor can atomically take an expired durable lease. Each takeover increases a fence value, so the store refuses all later writes from the stale executor. The direct in-memory ledger does not take over a live claim because it has no durable fence. Force resume never clears a possibly live claim. A crashed holder does not fence the run forever. | `TestResumeRefusesRunHeldByAnotherExecutor`, `TestResumeInterruptedRunTakesExpiredClaim`, `TestCancelRecoveredTakesExpiredClaim`, `TestMemoryLedgerDoesNotTakeOverLiveClaims`, `TestFencedLeaseTakeoverBlocksFreshAndRefusesStaleOwner`, `TestExpiredTakeoverFencesStaleOwnerWrite`, `TestClaimReleasedOnRunCompletion`, `TestClaimReleasedAfterHolderClose` | 2026-07-30 (lease and fence value added 2026-08-10) |
| INV-AG-14 | Safety | Deleting a run deletes its durable record, not just one process's projection. A tombstone is appended before prior rows are hard-deleted, so replay cannot resurrect the run, a second reader converges, and SQLite's rowid catch-up cursor remains monotonic. A later recreation of the same run ID starts after its tombstone, so a reader that observed deletion also observes the new run. Content is deliberately not deleted because references are shared and content-addressed. | `TestDeletedRunDoesNotResurrectInNextProcess`, `TestDeleteRunKeepsChangesCursorMonotonic`, `TestDeleteRunConvergesInASecondReader`, `TestDeleteRunAllowsSameIDToBeRecreatedAndCaughtUp`, `TestDeleteRunLeavesContentUntouched`, `TestDeleteRunOnMemoryBackend`, `TestRecoverDoesNotReportDeletedRunAsInterrupted` | 2026-07-30 (plan 24) |
| INV-AG-15 | Safety | Recorded content is retained unconditionally: no backend reclaims it when runs are deleted or by age, count, or size. This is deliberate: content-addressed references can be shared by several runs, and the same bytes remain in the session transcript, so retention is not a deletion path or privacy control. A surviving reference continues to resolve after deletion and after durable history is reopened. | `TestSharedContentRefSurvivesOneRunDeletion`, `TestContentStoreIsNeverReclaimed` | 2026-07-30 (plan 23, decided E) |
| INV-AG-16 | Safety | A spawn request fingerprint describes only requested work: task ID, handler, dependencies, input, timeout, budget, scope and permission. It excludes caller identity and dispatcher fields, so a repeat key can deduplicate across turns. A non-empty idempotency key is scoped by a fixed-length digest of the caller's session and role; a foreign principal receives a new run rather than another principal's run or a conflict. Callers with no session identity deliberately share the compatibility scope, and empty keys remain unkeyed. The scope and key index are process-local and are not persisted. | `TestSpawnAgentIdempotencyKeyDedupesAcrossTurns`, `TestSpawnIdempotencyKeyDedupesAcrossTurns`, `TestSpawnFingerprintIgnoresCallerIdentity`, `TestSpawnFingerprintCoversRequestedWork`, `TestSpawnConflictStillReportedForDifferentWork`, `TestSpawnIdempotencyScopeIsThePrincipal`, `TestSpawnForeignPrincipalGetsANewRun`, `TestSpawnForeignPrincipalIsIndistinguishableFromFirstUse`, `TestSpawnKeyNamespaceIsUnambiguous`, `TestSpawnScopeInputIsUnambiguous`, `TestSpawnWithoutCallerIdentityKeepsSharedScope` | 2026-07-30 (plan 22) |
| INV-AG-17 | Safety | Skill frontmatter unknown keys are rejected **at load**, and declared triggers reach the model-facing surface via `Definition.Triggers` | `TestLoadMarkdownRejectsUnknownFrontmatterKey`, `TestPromptRendersFromDefinitionTriggers`, `TestLoadMarkdownInjectsTriggersIntoPrompt` | 2026-07-30 |
| INV-AG-18 | Safety | The legacy `.ai` namespace is not compiled into production Go source: no non-test `.go` file contains a quoted `".ai"` element or a `.ai/` path prefix. Hostnames such as `openrouter.ai` and `api.z.ai` are deliberately not matched. Scope is production Go only - test files, docs, scripts and `testdata` are not covered, so a `.ai` string in those is not a violation of this row | `TestNoHardcodedLegacyNamespace` | 2026-07-30 |
| INV-AG-19 | Safety | A run resumed from a CLI surface is owned by the chat session's principal - `{chat.Session.SessionID, role:""}`, the pair `Dispatcher.Invoke` stamps on this session's tool calls - so the resuming session can inspect, join and cancel it and a foreign principal cannot. Resume fails closed when no session identity is available rather than starting a run nobody can control. An ephemeral principal is correct for the legacy compatibility path in `orchestrate.go`, which deliberately withholds later control, and wrong here | `TestResumeFromCLISurfaceUsesSessionPrincipal`, `TestResumedHandleRejectsForeignPrincipal`, `TestResumeRefusesWithoutSessionIdentity`, `TestResumeRunRegistersHandleWithResumingPrincipal` | 2026-07-30 |
| INV-AG-20 | Safety | Startup recovery classifies runs and never mutates one. There is no non-terminal `interrupted` status to write, and `RunStatusCompleted`/`Failed`/`Canceled` all make `ResumeInterruptedRun` refuse the run - so marking a run to stop re-reporting it would silently destroy the recoverability the report exists to advertise. Classification is therefore recomputed on every launch and is stable across repeated calls. Because a run abandoned days ago classifies identically to one interrupted moments ago, `Recover` carries `CreatedAt` and the startup report is age-filtered and bounded to a single line: older runs stay reachable through `/resume`, which shows each run's age so a long-dead one is recognisable as such rather than reading as something that just broke | `TestRecoverClassifiesWithoutMutatingRunStatus`, `TestInterruptedRunReportSkipsStaleRuns`, `TestInterruptedRunReportAnnouncesRecent`, `TestInterruptedRunReportIsBounded`, `TestInterruptedRunReportIgnoresTerminalRuns`, `TestFormatListedRunsShowsAge` | 2026-07-30 |
| INV-AG-21 | Safety | A run-level orchestration failure reaches the model in the tool payload - as `run_error` on `spawn_agent`/`join_run`, and as the affected task's own status in `dispatch_tasks`' per-task array - never as a Go error. `runtime.Dispatcher.failResult` replaces a failed tool's `Output` with a bounded `{"status":"failed","error":...}` envelope, pinned by `TestDispatcherFailSurfacesErrorReason`, so an error return destroys the payload while the error reason still reaches the model. `spawn_agent wait=run` and `join_run` are the same operation reached two ways and must report identically. A blocked dependency is always such a failure, naming both the blocked task and the failed dependency, and the ledger transition to `blocked` still happens first, because returning before it would leave the task queued forever and `tasksFromSnapshots` would re-dispatch it on resume. **Reporting a failure never costs the caller results.** A run always returns one result per task, including tasks that succeeded beside the failure, and there is deliberately no mode that returns less - the `partial_results` knob that used to choose was removed as inert. Two things hold this: the whole-call budget carries headroom over the longest task, because the agent loop arms the call's clock before the pool arms each task's and equal budgets made the outer deadline fire first; and when the caller's context expires before `Join` resolves, results are read back from recorded history instead of discarded, since `Join` returns `ctx.Err()` and never sets a result. A run cut off before any task reached an outcome reports the plain error instead, because a payload of `queued` tasks would read as success. The salvaged run is not cancelled and stays reachable via `inspect_agents`/`join_run` | `TestSpawnRunErrorsOnBlockedDependency`, `TestSpawnReturnsFullResultSetDespiteBlockedDependency`, `TestSpawnAgentReportsBlockedDependency`, `TestSpawnResultPayloadCarriesRunError`, `TestSpawnAgentSuccessLeavesRunErrorEmpty`, `TestDispatchTasksHangingTaskKeepsSiblingResults`, `TestDispatchOrchestrationBudgetOutlivesTaskBudget`, `TestPoolCancelStillReportsEveryTask`, `TestPoolRecordsBlockedTaskWhenDependencyFails`, `TestDispatcherFailSurfacesErrorReason` | 2026-07-30 |
| INV-AG-22 | Safety | Symbol reference tool `find_references` returns explicit `analysis unavailable` when no analyzer backend is available (non-Go workspace) rather than returning empty or falling back to text search | `TestFindReferencesRefusesWithoutAnalyzer`, `TestAnalyzerUnavailableWithoutGoMod` | |
| INV-AG-23 | Safety | The tool surface of `find_references` is project- and language-generic (rule 60); no Go-specific terms appear in parameter or description text | `TestToolSurfaceIsProjectAndLanguageGeneric` | |
| INV-AG-24 | Safety | Analyzer respects `GOPROXY=off` and does not attempt network access when resolving packages | `TestAnalyzerDoesNotReachNetwork` | |
| INV-AG-25 | Safety | Every tool in the default registry carries an explicit, recorded decision about its result size: it either declares `tools.ResultBudgetBytes()` - which the dispatcher reads to derive that tool's OWN output backstop (INV-AG-27), with `runtime.DeriveOutputCeiling` setting the global cap those per-tool ceilings may only tighten below - or its name appears in `unbudgetedDefaultTools` with the reason it needs none. The gate enumerates the registry, so a tool added later fails until one of the two is done. A count cap is **not** a byte bound: `list_dir` capped entries, `glob`/`grep` capped matches, `write_file` was sized by the file already on disk, and each produced results past the ceiling, which the dispatcher DESTROYS rather than truncates. Bounds are proved against true worst-case inputs (255-byte name components, paths approaching PATH_MAX), never against typical sizes - that estimate is what let this class survive two prior audits. A declared budget bounds tool CONTENT; fixed framing may ride above it within `outputCeilingSlack`, and the newly budgeted tools additionally pay for their truncation notice out of the budget so the loop never has to tail-cut their honest framing. Network-derived content is no longer exempt: `search` and `extract` bound the provider response and declare it (INV-AG-26) | `TestEveryDefaultToolHasARecordedResultSizeDecision`, `TestWorstCaseWorkspaceToolOutputStaysWithinBudget`, `TestDeclaredBudgetsAreCoveredByTheDerivedCeiling`, `TestRegression_ListDirLongNamesNotDestroyedByDispatcherCeiling`, `TestRegression_GlobDeepPathsNotDestroyedByDispatcherCeiling`, `TestRegression_WriteFileOverwriteDiffNotDestroyedByDispatcherCeiling`, `TestIntegration_LargeGlobReachesModelTruncatedNotDestroyed`, `TestIntegration_LargeListDirReachesModelTruncatedNotDestroyed`, `TestIntegration_LargeOverwriteDiffReachesModelTruncatedNotDestroyed`, `TestReadClassBudgetsComeFromMaxReadBytes`, `TestReadClassToolsDoNotDeclareLoopTruncationBound` | 2026-07-31 |
| INV-AG-26 | Safety | Network-derived tool content is BOUNDED, never truncated. The `search` tool and `extract` bound their output by one configured number (`[tools] max_tavily_response_bytes`, default 4 MiB) which they enforce on the provider response body AND on EVERY composed return path - Tavily search, Tavily extract, extract's empty-content echo, the free-engine fallback and its no-results case - and declare via `tools.ResultBudgetBytes()`. Guarding the wire alone is NOT sufficient and the omission is a live defect, not a theoretical one: composition can outgrow the body it came from, because `search` writes a 4-byte bullet per result against a 3-byte empty JSON object (up to 5/3 amplification), formats the model-supplied query into a header with `%q` (which expands a byte to four), and `extract`'s empty-content path echoes a model-supplied URL list bounded by the request rather than the response. Every return path is guarded so `ResultBudgetBytes()` is EXACT rather than exact-modulo-framing, which is what lets the derived per-tool ceiling clear an honest result. Nothing is cut anywhere on these paths: `formatWebResult` no longer clips snippets to 150 bytes, so each result's text - not merely the answer field - reaches the model whole. An over-bound result is REFUSED with an explicit error naming the bound and the key that raises it, and the refusal is never laundered into free-engine results by `search`'s fallback. There is no unlimited setting: an undeclared result is precisely what the dispatcher destroys. An install with no provider key declares only the free-engine composition bound, so its ceilings are unchanged | `TestRegression_TavilyExtractLargePageReachesModelWhole`, `TestRegression_TavilySearchLargeAnswerReachesModelWhole`, `TestRegression_TavilyResultsClearTheDerivedCeiling`, `TestSearchRefusesOverBoundResponseBody`, `TestSearchRefusesOverBoundComposedResult`, `TestSearchRefusesOverBoundHeaderExpansion`, `TestExtractRefusesOverBoundResponseBody`, `TestSearchFreeEngineResultIsGuarded`, `TestExtractEmptyContentPathIsGuarded`, `TestSearchResultContentSurvivesWhole`, `TestFreeEngineSnippetSurvivesWhole`, `TestSearchBudgetRefusalIsNotSwallowedByFreeEngineFallback`, `TestSearchReturnsUnderBoundAnswerWhole`, `TestExtractReturnsUnderBoundContentWhole`, `TestKeylessRegistryDeclaresNoProviderSizedBudget`, `TestKeyedRegistryDeclaresConfiguredBudget`, `TestTavilyResponseBudgetDefaultMatchesConfig`, `TestMaxTavilyResponseBytesDefaultsToBuiltIn`, `TestMaxTavilyResponseBytesFloorIsALoadError`, `TestMaxTavilyResponseBytesCeilingIsALoadError`, `TestWireBoundIsExactAtTheBoundary`, `TestWireBoundFallsBackToTheDefault` | 2026-07-31 |
| INV-AG-27 | Safety | A tool's output ceiling is derived from ITS OWN declared `tools.ResultBudgetBytes()` - `max(declaredBudget, outputCeilingFloor) + Policy.MaxInputBytes + outputCeilingSlack` - and is never raised by another tool's budget. `Policy.MaxOutputBytes` is a HARD global cap: the enforced bound is the minimum of the two, so a per-tool ceiling may only ever tighten, never widen, and an explicit operator bound cannot be exceeded by a declaration. Before this, one backstop was computed as the max over every registered budget and applied to every tool, so a single generous budget bought every other tool the same slack - raising `[tools] max_read_bytes` to 2 MiB handed `dispatch_tasks`, `delegate` and the ledger tools a 2 MiB+ ceiling none of them justified, while a tool that declared 256 KiB could emit megabytes and stay under the backstop its own declaration said should have stopped it. Kinds with nothing to derive from (`Skill`, `Subagent`) and handlers installed through the bare `Register` path keep `Policy.MaxOutputBytes`; a tool that declares nothing gets the floor-derived value (331776 on the Policy defaults), which is exactly what the old global gave it on the shipped config, so the change tightens no undeclared tool below where it already sat. The ceiling is recorded in the SAME critical section that installs the handler and read in the same one that resolves it, so an invocation can never execute a handler under a bound belonging to another registration. Scoped sub-agent dispatchers derive per-tool ceilings on the same rule, including the nil-parent path where `parentPolicy()` returns a zero policy. An over-ceiling failure names the capability, its result size and the bound it broke - sizes and the name only, no content - and deliberately avoids the substrings `internal/cli`'s `statusFromErr` matches to classify a task as canceled or timed out | `TestPerToolCeilingIsNotRaisedByAnotherToolsBudget`, `TestPerToolCeilingClearsEveryDeclaredBudget`, `TestPerToolCeilingNeverExceedsTheGlobalCeiling`, `TestUndeclaredToolGetsFloorDerivedCeiling`, `TestExplicitPolicyCapsEveryPerToolCeiling`, `TestNonToolKindsUseThePolicyCeiling`, `TestBareRegisterKeepsThePolicyCeiling`, `TestConcurrentRegisterToolAndInvoke`, `TestOutputCeilingFailureNamesTheToolAndTheCeiling`, `TestRegisteredToolCeilingMatchesTheRegistryResolvedTool`, `TestFindReferencesCeilingClearsItsDeclaredBudget`, `TestScopedDispatcherDerivesPerToolCeilings`, `TestScopedDispatcherWithoutParentDerivesPerToolCeilings`, `TestSessionToolCeilingsAreFloorDerived` | 2026-07-31 |
| INV-AG-28 | Safety | The selectable model catalog is finite, explicit, provider-qualified, and credential-aware; a session switch or restore publishes provider, model, profile, completer, and dispatcher as one idle binding generation, while in-flight turns retain their captured generation and nested agents receive the selected prompt budget | `TestModelCatalogIsProviderQualifiedAndStable`, `TestModelCatalogRejectsMissingActiveModelsAndRegistryFallback`, `TestModelCatalogRejectsDuplicateNamesAndCaseCollidingProviders`, `TestIntegrationModelBindingKeepsTurnGeneration`, `TestIntegrationModelBindingClosesPreviousDispatcherGeneration`, `TestIntegrationLoadBuildsBindingBeforeHistory`, `TestIntegrationModelBindingRequiresFactoryForConfiguredCatalogLoad`, `TestIntegrationPlainTurnUsesPreparedPromptSnapshot`, `TestIntegrationAgentPreflightRejectsOversizedPrompt`, `TestOneShotHandlerPreflightAndOutputReserve`, `TestMultiStepHandlerCarriesPromptBudgetToAgentLoop`, `TestIntegrationModelBindingBudgetCommandParityAcrossREPLAndTUI` | 2026-07-31 |
| INV-AG-29 | Safety | Workspace configuration cannot widen an agent's effective tool name set beyond what the user authorized, nor inject untrusted content into gated prompt surfaces: workspace agent files under `.mivia/agents/` always load (they replace agent-prompt.md) but cannot shadow same-named user agents; the user-owned `load_workspace_config` gate still controls workspace mivia.toml system prompts and project skill handlers; workspace cannot loosen `[agents.guardrails]`; cannot lower the compiled mandatory denylist; agent TOML rejects unknown keys; definitions resolve to immutable snapshots; default root agent name is `mivia` when present; root vs spawned scope differ (privileged tools retained only at root); excluded tools are denied at execution | `TestWorkspaceAgentsAlwaysLoad`, `TestWorkspaceGlobalSettingsIgnored`, `TestGateKeepsUserMeaningWhenWorkspaceIsHome`, `TestWorkspaceAgentsRefusedWhenWorkspaceIsHome`, `TestAgentSkillsKeyParsed`, `TestAgentSymlinkFileRefused`, `TestAgentHardlinkRefused`, `TestAgentResolve_ToolsAddExtendsParent`, `TestResolve_EvalOrder`, `TestValidateAgainstCatalogue_UnknownToolName`, `TestAgentResolve_EmptyToolsetRefused`, `TestWorkspaceAgentCannotShadowUserAgent`, `TestUserGuardrailsCannotBeLoosenedByWorkspaceAgent`, `TestAgentDescriptionSanitized`, `TestAllToolNamesMatchesFullRegistry`, `TestScopedRegistry`, `TestMandatoryDenylistMatchesPrivilegedMarker`, `TestMandatoryDenylist_RootExempt_SpawnedFiltered`, `TestWorkspaceSystemPromptStrippedWhenGateOff`, `TestUserConfigSystemPromptAlwaysLoaded`, `TestWorkspaceSkillHandlersNotRegisteredWhenGateOff`, `TestWorkspaceSkillHandlersRegisteredWhenGateOn`, `TestRootSession_AgentFlag`, `TestAgentScopedLoopCannotWriteFile`, `TestAgentNameCollidesWithSkill`, `TestDefaultAgentIsMiviaWhenPresent`, `TestDispatcherAgreesWithSessionRegistryAfterAttach`, `TestDispatcherAgreesAfterAgentSwitch` | 2026-08-03 |
| INV-AG-30 | Safety | Agent–skill binding (plan 06): skill frontmatter `tools` is parsed and published on `Definition.Tools` (nil omit vs non-nil empty `tools: []`); agent-file `skills` resolves with root omit=all, explicit `[]`=none, inherited omit=parent; unknown skills rejected; workspace skills cannot silently shadow user bindings and require the workspace gate when project-only; when the workspace gate is off, project skill sources are not loaded so a same-named project skill cannot erase a user skill; an explicit task skill is enforced against the selected task agent's immutable policy at dispatch and again on resume/retry; `agent.Tools ⊇ skill.Tools` is non-vacuous when skill tools are declared; isolation holds under concurrent instances and model-switch rebuilds. Nested agents cannot synthesize tasks (privileged tools stripped) so v1 is root fan-out only | `TestSkillToolsParsedAndPublished`, `TestSkillToolsEmptyListIsNonNilEmpty`, `TestAgentSkillAllowlist_OmittedAllowsAll`, `TestAgentSkillAllowlist_EmptyAllowsNone`, `TestAgentSkillsInherited`, `TestUnknownSkillRejected`, `TestWorkspaceSkillCannotShadowUserBinding`, `TestWorkspaceGateRequired`, `TestUserSkillSurvivesProjectShadowWhenWorkspaceGateOff`, `TestAgentSkillAllowlist_PerInstance`, `TestSkillCannotBypassAgentSelection`, `TestSkillToolsSubsetOfAgentTools`, `TestConcurrentAgentSkillInstances`, `TestAgentSkillBindingSurvivesModelSwitch`, `TestResumeRechecksAgentAccess`, `TestSkillToolsSubsetNonVacuousFixture`, `TestNewSessionDispatcher_SkillScopeGatesRegistration` | 2026-08-01 |
| INV-AG-31 | Safety | Every `dispatch_tasks` and `spawn_agent` task has one explicit canonical agent definition and optional separate skill. Handler names, task names, roles, built-in runners, and unknown task fields are rejected before coordinator creation. The task persists agent name, immutable definition digest, and skill as work metadata; retry/resume routes only through the current named-agent handler that verifies the digest and rechecks the selected agent's skill policy. Idempotency fingerprints include all three fields. Each invocation derives isolated tool and loop state from its immutable definition and cannot widen the caller's scoped registry. | `TestAgentFieldRequired`, `TestHandlerAndNameSelectorsRejected`, `TestBuiltInRunnerCannotSelectAgent`, `TestAgentEnumInParameters`, `TestSkillDoesNotReplaceAgent`, `TestResumeFailsWhenAgentDefinitionChangesBeforeLedgerMutation`, `TestSpawnFingerprintCoversRequestedWork`, `TestTaskSnapshotRoundTripsNewFields` | 2026-08-01 |

| INV-AG-34 | Safety | Lifecycle hooks load from TWO configs and they ADD rather than replace: the user config at its fixed path, then the workspace's own `.mivia/mivia.toml`. Hooks are the one setting mivia merges across layers, because a project's formatter and a user's global gate are two hooks rather than competing answers, and a workspace file that replaced the user's would silently disarm a gate by opening a repository. **User groups are ordered first**, so a gate the operator wrote answers before a repository's does. `$MIVIA_CONFIG` selects the general config and supplies no hooks - a third command-executing file chosen by an environment variable would make "what can run here" depend on how mivia was launched. **A declared hook runs, with no confirmation step**, on every surface. For a project hook this is a deliberate, disclosed trade rather than an oversight: a cloned repository CAN execute its hooks on first launch, and what stands in for a prompt is disclosure that cannot be missed - startup names every armed hook and says outright when one came with the workspace, `/hooks` marks each `[user]` or `[project]`, and every execution gets a transcript row. A faulty workspace config NEVER fails startup - a symlink, an oversized file, or one that does not parse is reported and contributes nothing while the user's own hooks carry on - because any repository can ship that file and letting one break every session in its directory would hand a clone a denial of service; the user config keeps the opposite treatment, failing closed, because it is the operator's own file. There is no `trust` key, no trust store, no definition hash and no `--bypass-hook-trust` - the flag is accepted and ignored with a notice so a CI config carrying it still starts, and `/hooks trust` answers that the concept was removed rather than reporting an unknown argument. A headless run executes exactly what an interactive one does. What replaces the prompt is DISCLOSURE, and it is mandatory: startup names every armed hook, `/hooks` lists event, matcher, argv and resolved timeouts, and the listing states that arming names a PROGRAM and does not track that file's contents - editing the script body changes what runs. There is still no operator tier, because one requires a file the user (and the agent running as them) cannot write, and nothing this product installs creates such a file. Hooks execute as an explicit `argv` with no shell, no `PATH` lookup and no interpolation of tool-derived values - those reach the hook through `MIVIA_*` env vars and the stdin JSON, which are never re-parsed as syntax - and never re-enter `Dispatcher.Invoke`, with `internal/hooks` importing neither `internal/runtime` nor `internal/tools` and the re-entrancy guard scoped to the context rather than the process, so one call's hook cannot suppress a concurrent call's gate. `PreToolUse` is the only blocking event, fires only for `Kind == Tool`, propagates to derived subagent dispatchers via `Policy` - a subagent's tool call fires the parent's gate, is BLOCKED when it denies, and receives the reactive event's context framed exactly as the root loop's is, because a gate a subagent escapes is not a gate, defaults to blocking on timeout or on a handler that could not start, denies rather than coerce an unsupported `permissionDecision` or the wrong event's flat shape onto permission, rejects `updatedInput`, and returns status `blocked` - distinct from `failed`, with a fixed error text that avoids the substrings `statusFromErr` matches - while the reason reaches the model verbatim in the payload. An ALLOWING `PreToolUse` hook keeps its `additionalContext`, read nested or flat, and it is merged with the reactive event's rather than discarded; the merged text answers to the one `MaxHookContextBytes` bound. Hook output is carried in its own bounded `Result.HookContext`, truncated on a rune boundary with a notice and never appended to `Result.Output`, so per-tool output ceilings (INV-AG-25/26/27) and `meta.OutputHash`/`OutputPreview` remain exact. The dispatcher only BOUNDS that text; framing is applied where the model's view is assembled, in `agent.FrameHookOutput`, which wraps it in `<lifecycle-hook-output>`…`</lifecycle-hook-output>` with an in-band statement that it is advisory - in-band because a `.mivia/agents/` definition replaces the compiled prompt wholesale. **The framed text cannot forge its own frame**: any string a model could read as either tag - either case, either direction, with or without inner whitespace or attributes, even split across lines - is replaced with a shorter fixed token before assembly, so escaping only ever shrinks the payload and a hook cannot buy model-visible bytes past `MaxHookContextBytes`. Framing overhead is a constant, never a multiple of the payload. This is attribution, not a sandbox. Separately from the model's copy, EVERY handler that executed is recorded on `Result.HookRuns` and emitted as an `agent.EventHook` transcript row - including a run that produced no output at all, because a mis-typed matcher that selects nothing is otherwise indistinguishable from a working hook | `TestMiviaConfigEnvDoesNotRelocateHookSource`, `TestHooksSourceRefusesSymlinkedUserConfig`, `TestOversizedWorkspaceConfigIsBoundedAndReported`, `TestRejectionTable`, `TestBareProgramNameIsNotFoundViaPATH`, `TestShellMetacharactersArriveAsLiteralArgv`, `TestToolFilenameWithShellSyntaxDoesNotExecute`, `TestArgvResolvesAgainstConfigDirectory`, `TestHooksPackageDoesNotImportRuntimeOrTools`, `TestHooksTransitiveDepsExcludeRuntimeAndTools`, `TestHooksPackageNamesNoShell`, `TestHookReentryIntoTheDispatcherDoesNotRecurse`, `TestConcurrentInvocationKeepsItsGateWhileAnotherHookRuns`, `TestHooksFireOnlyForKindTool`, `TestDeduplicatedInvocationFiresNoHook`, `TestScopedSubagentDispatcherInheritsHookFuncs`, `TestPreToolUseTimeoutBlocksByDefault`, `TestUnstartableHandlerUsesTheOnTimeoutVerdict`, `TestExplicitOnTimeoutAllowWarnsInsteadOfBlocking`, `TestUnsupportedPermissionDecisionIsDeniedNotAllowed`, `TestUpdatedInputIsRejectedNotIgnored`, `TestExitTwoBlocksAndIgnoresStdoutJSON`, `TestUnparseableDecisionJSONFallsBackToExitCode`, `TestPreToolUseDenyReturnsBlockedNotFailed`, `TestBlockedErrorAvoidsCancellationSubstrings`, `TestBlockedCallReleasesWaitersAndClearsTheActiveMarker`, `TestPostToolUseCannotBlock`, `TestStopHookCannotBlockTheTurn`, `TestStopEventIsFiredFromExactlyOneProductionSite`, `TestNilHookFieldsLeaveResultsUnchanged`, `TestPostToolUseContextLandsInHookContext`, `TestHookContextDoesNotChangeTheAuditHashOrPreview`, `TestOversizedHookContextIsTruncatedAndKeepsTheResult`, `TestPostToolUseRunsAfterTheCallTimeoutExpired`, `TestOversizedStdoutIsTruncatedWithNotice`, `TestTruncationNeverSplitsARune`, `TestBlockReasonNeverSplitsARune`, `TestHookContextIsAppendedAsAnAttributedBlock`, `TestStopHookOutputIsBounded`, `TestInstructionLikeHookOutputStaysOutOfTheToolOutput`, `TestHookContextIsWrappedInAStructuralDelimiter`, `TestFramingStatesHookOutputIsNotInstructions`, `TestHookContextCannotForgeTheClosingDelimiter`, `TestNeutralizingForgedDelimitersNeverGrowsTheContext`, `TestNeutralizingAnUnterminatedTagDoesNotEatTheRestOfTheOutput`, `TestFramingOverheadIsConstant`, `TestEveryConfiguredHookRunsWithoutConfirmation`, `TestStartupNamesEveryArmedHook`, `TestNoHooksConfiguredAnnouncesNothing`, `TestHooksListStatesTheScriptBodyIsNotTracked`, `TestHooksTrustExplainsThatConfirmationIsGone`, `TestStaleBypassFlagIsAcceptedAndReported`, `TestAllowingPreToolUseHookKeepsItsAdditionalContext`, `TestOutcomeRecordsEveryRunIncludingSilentOnes`, `TestABlockingRunRecordsItsReason`, `TestPreToolUseContextOnTheAllowPathReachesTheModel`, `TestPreAndPostContextAreBothKeptAndStayBounded`, `TestSilentHookStillRecordsARun`, `TestABlockedCallCarriesItsHookRuns`, `TestEveryHookRunIsEmittedIncludingSilentOnes`, `TestABlockingHookRunSaysItBlocked`, `TestAWarningRunSurfacesTheDiagnostic`, `TestNoHookRunsEmitNothing`, `TestIntegrationConfiguredHookRunsWithNoConfirmationStep`, `TestIntegrationHookOutputReachesTheModelFramedAndUnforgeable`, `TestIntegrationPreToolUseAdvisoryContextReachesTheModel`, `TestIntegrationPreToolUseBlockStopsTheToolAndIsAttributed`, `TestIntegrationSilentHookIsStillVisibleToTheOperator`, `TestIntegrationAMissingHookScriptWarnsAndKeepsTheResult`, `TestIntegrationHooksDoNotFireForSubagentDispatch`, `TestHooksLoadFromBothUserAndProjectConfigs`, `TestProjectHooksLoadWithNoUserConfigAtAll`, `TestProjectHookConfigFailsSoftAndRefusesLinks`, `TestProjectHooksLoadAlongsideUserHooks`, `TestListingMarksProjectHooksAndWarnsAboutThem`, `TestStartupCallsOutProjectHooksSeparately`, `TestUserOnlyHooksGetNoProjectNotice`, `TestAnInvalidProjectConfigWarnsInsteadOfFailingStartup`, `TestIntegrationProjectHookRunsFromTheWorkspace`, `TestIntegrationUserAndProjectHooksBothFireUserFirst`, `TestIntegrationAProjectHookCanBlockACall`, `TestSubagentToolCallFiresTheParentPreToolUseHook`, `TestSubagentToolCallIsBlockedByTheParentGate`, `TestSubagentToolCallCarriesPostToolUseContext`, `TestSubagentWithNoParentDispatcherRunsNoHooks`, `TestRepoHookScriptsExistAndAreExecutable`, `TestRepoPreToolUseGateBlocksOnNoVerdict`, `TestRepoHookMatchersAreAnchored`, `TestRepoBypassGateActuallyBlocksABypassArgv`, `TestRepoDestructiveGateBlocksLossAndAllowsWork` | 2026-08-01 (plan 44 phases 1-3) |

| INV-AG-32 | Safety | Context-enabled turns use one preparation transaction across plain chat, root agent loops, and nested loops; durable publication is fenced and never falls back to legacy JSONL, while source projection and compaction progress events omit system prompts, tool arguments, hidden content, and summary payloads. The omission is scoped to those two surfaces: the checkpoint's own active context is the conversation the model works from, and per INV-AG-39 it DOES carry a compaction's summary. | `TestPlainTurnUsesPreparationTransaction`, `TestCheckpointFailureDoesNotFallbackToJSONL`, `TestAgentTurnDiscardsFailedPreparation`, `TestMultiStepHasNoCheckpointCapability`, `TestContextCompactionInvariants`, `TestEmitCompactionAfterCommitOnly`, `TestContextEnabledTurnCommitsSQLite`, `TestPlainChatInjectsSummaryIntoStreamRequest` | 2026-08-15 |
| INV-AG-33 | Safety | **What one session persists can never stop another session from persisting.** A workspace's context store is shared by every `mivia chat` run in it, so a durable key derived from content alone is a cross-session denial of service: `context_payloads.ref` is a GLOBAL primary key whose stored owner columns must equal the writing principal, so while the key was the bare content digest the first session to persist a byte sequence owned it forever and every later session that produced identical content - a short message retyped into a second run - had its ENTIRE commit transaction rejected as `checkpoint conflict`, leaving no source events, no checkpoint and no `context_operations` row, and silently losing the turn from both durable history and the in-memory conversation. Payload keys are therefore scoped to their owning principal with a length-prefixed owner tuple, so principals differing only in where their field boundaries fall cannot collapse onto one key, while content repeated within ONE owner still resolves to one row. Sharing a row across owners is deliberately NOT the alternative: reads gate on the row's owner columns, so one session resolving another's payload is the boundary this package exists to hold. Keys are never re-derived for lookup - every read resolves the reference string stored on its source event - so rows written under the old derivation keep resolving and no migration is needed. Separately, every durable conflict now names its class (operation key, checkpoint identity, source sequence, payload owner) rather than returning a bare sentinel: a contextless `checkpoint conflict` is what made this defect cost a multi-session investigation | `TestSourcePayloadRefIsScopedToItsOwner`, `TestSourcePayloadRefDeduplicatesWithinOneOwner`, `TestSourcePayloadRefOwnerEncodingIsUnambiguous`, `TestTwoSessionsPersistIdenticalContent`, `TestLegacyPayloadRowStillResolves`, `TestIntegrationSecondSessionInSharedStorePersistsRepeatedTurns`, `TestIntegrationOneSessionRepeatingItselfStillCommits`, `TestIntegrationSharedStoreSessionsKeepSeparatePayloadRows` | 2026-08-02 |

| INV-AG-35 | Safety | **A durable size bound may never destroy work the agent already finished.** Context publication is ONE transaction, so exceeding any volume bound refuses the whole turn - no source events, no checkpoint, no `context_operations` row - and because an active context only grows, one refusal wedges the session permanently and history stops persisting with no way back. The bounds were compiled-in constants sized for a demo (32 KiB of active context, 64 KiB per message) while the shipped model catalogue carries 200k-1M token windows and `read_file`/`grep`/`run_command` results are uncapped by default, so the first turn in which the agent did real work destroyed the conversation - and `CommitRequest.Validate` allowed 64 MiB for the very bytes `CheckpointRecord.Validate` refused above 32 KiB, a 2048x disagreement about one field. Every volume bound is therefore declared in exactly one place (`contextstate.Limits`), ships UNCAPPED, and is operator-owned through `[context]`, installed process-wide at startup beside the redaction policy; a process that configures nothing runs uncapped rather than under a compiled-in guess, and a negative ceiling clamps to uncapped rather than refusing a turn. A zero bound means no limit everywhere - the one predicate (`Exceeds`) is shared by every layer so a zero can never read as "allow nothing". Bounds that describe SHAPE rather than volume stay compiled in and are deliberately out of scope: identifier and reference lengths, the source-range span that keeps range arithmetic honest, and the host-authored summary/audit metadata envelopes Workspace identity is the resolved DIRECTORY, not how its path was spelled: `mivia chat` passes "." when no --workspace is given, so every project on a machine hashed to one durable workspace id, and `chat_sessions` is keyed on it | `TestIntegrationLargeToolTurnStillCommits`, `TestDurableLimitsAreUncappedByDefault`, `TestContextLimitsAreOperatorOwned`, `TestNegativeContextCeilingClampsToUncapped`, `TestSourceEventValidation`, `TestContextWorkspaceIDIsTheResolvedDirectory`, `TestContextWorkspaceIDSeparatesDirectories`, `TestLineModeReportsTurnFailures`, `TestLineModeStillReportsCancellation`, `TestCancellationCanReplaceOnlyCancellationErrors`, `TestOneShotPrintsReplyBeforeAutosaveFailure` | 2026-08-10 |
| INV-AG-36 | Safety | **Reasoning control only ever ADDS request fields.** With a model's reasoning level unset the serialized body is byte-identical to the pre-reasoning shape, so every existing configuration is untouched even on a provider that now has a default dialect. With it active, exactly the resolved dialect's documented fields are added and NO sampling parameter is removed: the earlier premise that reasoning models reject `temperature` was disproved against current provider documentation (DeepSeek accepts and ignores sampling in thinking mode, Z.AI's own active-thinking example sends `temperature`, OpenRouter advertises sampling per model), so suppressing it would itself have changed valid requests to avoid a 400 that does not occur. The dialect resolves request-first then to the provider's vetted default; when neither resolves the client sends NOTHING rather than guessing a wire shape, which is why config refuses an active level with no resolvable dialect - a key that looks applied and does nothing is worse than a load error. DeepSeek's vetted default is `thinking_effort`, and its client requires `reasoning_content` replay (`RequiresReasoningReplay`), which `provider.Message` carries. Reasoning merges AFTER `ExtraBody` so an active model-scoped level deterministically beats a colliding static key instead of losing a map-iteration coin flip. The dial belongs to the model binding, so every request path sends the same fields for the same binding - both plain-chat builders, the agent loop, one-shot and multi-step subagents, routed agents on their OWN model rather than the session's, and the non-streaming fallback inside `readStream` that rebuilds a request field by field. A model declares WHICH efforts it offers (`reasoning_efforts`) and `reasoning` is the default, which must be a member: the set is the source of truth, so a default outside it would be a value `/effort` could never return to and a default with no set would be a second spelling of the same config, and the dialect requirement keys on the declared set rather than the default because any listed level can be activated at runtime. A `/effort` override is confined to that declared set and DIES WITH THE BINDING - it folds into the captured binding rather than being threaded separately, so it cannot change mid-turn or drift from the model it was chosen for, and switching models restores the new model's default instead of carrying a depth the user picked for something else. The paths that do not capture a binding (one-shot, multi-step and skill subagents, and a routed agent that follows the session) read the session's effective dial through a live accessor at invoke time, because a value frozen at dispatcher construction would leave delegated work at the depth the session started with | `TestUnsetLevelLeavesBodyUnchanged`, `TestActiveReasoningKeepsSamplingParameters`, `TestDialectBodyFields`, `TestActiveLevelWithNoResolvableDialectEmitsNothing`, `TestActiveReasoningWinsOverCollidingExtraBody`, `TestExtraBodySurvivesWhenNoLevelIsActive`, `TestBuildingARequestDoesNotMutateTheCaller`, `TestStreamFallbackCarriesReasoning`, `TestConstructorDefaultsMatchTheConfigFacingTable`, `TestActiveReasoningRefusedWithoutAResolvableDialect`, `TestActiveLevelWithExplicitNoneDialectIsRefused`, `TestDefaultDialectCoversVettedProvidersOnly`, `TestShippedExampleConfigLoads`, `TestIntegrationPlainTurnCarriesModelReasoning`, `TestIntegrationAgentTurnCarriesModelReasoning`, `TestIntegrationPlainAndAgentTurnsAgreeForOneBinding`, `TestIntegrationSwitchingToANonReasoningModelClearsTheDial`, `TestIntegrationSwitchingToAReasoningModelActivatesTheDial`, `TestIntegrationSelectModelClearsThePreviousModelsReasoning`, `TestLegacySwitchPathClearsThePreviousModelsReasoning`, `TestModelSpecDecodesDeclaredEfforts`, `TestDefaultEffortMustBeAmongTheDeclaredEfforts`, `TestDefaultEffortWithoutADeclaredSetIsRejected`, `TestDeclaredEffortsRequireAResolvableDialect`, `TestDeclaredEffortsWithoutADefaultAreLegal`, `TestIntegrationEffortOverrideReachesThePlainRequest`, `TestIntegrationEffortOverrideReachesTheAgentRequest`, `TestEffortOutsideTheDeclaredSetIsRefused`, `TestEffortOverrideDiesWithTheBinding`, `TestSwitchingBackRestoresTheModelDefault`, `TestSelectModelClearsTheEffortOverride`, `TestEffortCannotChangeWhileWorkIsActive`, `TestIntegrationModelDialogShowsTheDefaultEffort`, `TestIntegrationEffortDialogListsDeclaredEffortsInConfigOrder`, `TestIntegrationEffortDialogExplainsAModelThatOffersNothing`, `TestIntegrationEffortDialogSelectionChangesTheNextRequest`, `TestIntegrationEffortDialogIsATrueModal`, `TestOneShotHandlerCarriesReasoning`, `TestMultiStepHandlerCarriesReasoning`, `TestRoutedAgentUsesItsOwnModelsReasoning`, `TestRoutedAgentOnANonReasoningModelSendsNothing`, `TestSessionReasoningComesFromTheSelectedModel`, `TestIntegrationEffortOverrideReachesNestedHandlers`, `TestIntegrationEffortOverrideAndRoutedAgents` | 2026-08-03 |
| INV-AG-37 | Safety | The CURRENT namespace directory name is single-sourced. `workspace.Namespace`/`workspace.NamespacePath` are the only code routes to the namespace directory: production Go outside `internal/workspace` never names it except in allowlisted user-facing text (slash-command log, CLI help, hook notices, prompt self-maintenance, config error). The legacy `.ai` name has its own guard (INV-AG-18). Without the guard a rename of `Namespace` silently diverges surfaces: config read from the old name while agents/skills load from the new, orphaned worktree markers, sandbox no longer excluding the control directory, delivery policy silently unapplied | `TestNamespaceNameSingleSourced` | |
| INV-AG-38 | Safety | get_diagnostics is registered only when `[tools] diagnostics_commands` configures at least one command AND the run allowlist is non-empty AND the default command's argv[0] resolves on the effective allowlist (PATH-probed) — no zombie advertisement: unset or empty leaves the tool unregistered (backward compatible; every pre-existing config keeps loading). Every map entry is validated AT LOAD like the run_command gate: non-empty argv, a bare argv[0] (no path separator) on the effective allowlist (`run_allowlist_only` when set, else `run_allowlist`, minus `run_blocklist`), and command names non-empty and unique ignoring case — a diagnostics tool that could never register cannot load clean. The v1 `diagnostics_command` alias is DEPRECATED and folds into the map's reserved "default" entry (then is cleared before validation); setting BOTH `diagnostics_command` and `diagnostics_commands` is a LOAD error, because the map is the one surface. Registration probes only the default command (the reserved "default" entry, else the sole configured command; with several entries and no default the tool still registers and every selection is gated at Execute). Selection is gated every time at Execute: the "command" argument picks the entry (falling back to the default), an unknown name or an omitted command with several entries and no default is refused with an envelope-level error, and the selected argv is re-resolved against the allowlist — get_diagnostics adds no execution authority beyond run_command. The envelope names the SELECTED command (`command_name`) and its argv, never the default's. The whole captured output (stdout+stderr) is redacted BEFORE parsing/normalization, so a credential hidden inside a parsed file field can never reach a row. An over-budget composed envelope is REFUSED with a bounded error envelope naming the bound — never tail-cut, never invalid JSON — and the declared result budget (256 KiB default) feeds the derived per-tool output ceiling without ever raising the global cap. The model-facing surface stays project- and language-generic (rule 60) | `TestDiagnosticsConfigAliasFoldsIntoDefaultMapEntry`, `TestDiagnosticsConfigAliasAndMapBothSetIsLoadError`, `TestDiagnosticsConfigMapKeyValidation`, `TestDiagnosticsConfigMapEntryValidation`, `TestDiagnosticsConfigMapValidEntriesLoad`, `TestDiagnosticsConfigMapDefaultNameExplicit`, `TestDiagnosticsConfigMapRunAllowlistOnlyMembership`, `TestDiagnosticsConfigMapRunAllowlistOnlyPasses`, `TestDiagnosticsConfigPathShapedArgv0IsLoadError`, `TestDiagnosticsConfigArgv0NotInRunAllowlistIsValidationError`, `TestDiagnosticsConfigArgv0InRunAllowlistPassesValidation`, `TestDiagnosticsConfigEmptyPassesBackwardCompatible`, `TestDiagnosticsConfigRunAllowlistOnlyMembership`, `TestDiagnosticsConfigRunAllowlistOnlyPasses`, `TestGetDiagnosticsRegistrationConditions`, `TestGetDiagnosticsCommandSelectionRunsNamedCommand`, `TestGetDiagnosticsCommandOmittedRunsDefault`, `TestGetDiagnosticsCommandUnknownFails`, `TestGetDiagnosticsNoDefaultMultipleCommandsFails`, `TestGetDiagnosticsEnvelopeReflectsSelectedCommand`, `TestIntegrationDiagnosticsCommands`, `TestGetDiagnosticsExecuteRunsRealArgv`, `TestGetDiagnosticsRefusesNonAllowlistedArgv`, `TestGetDiagnosticsOverBudgetRefusesEnvelope`, `TestGetDiagnosticsTimeoutReturnsErrorEnvelope`, `TestGetDiagnosticsRedactsCredentialToken`, `TestGetDiagnosticsMaxRowsDropsRows`, `TestGetDiagnosticsCapability`, `TestGetDiagnosticsBudgetClampedToConfiguredCap`, `TestGetDiagnosticsBudgetUnclampedWithoutCap`, `TestGetDiagnosticsSurfaceIsProjectAndLanguageGeneric`, `TestDeriveOutputCeilingCoversDiagnosticsCommands` | 2026-08-12 |
| INV-AG-39 | Safety | A compaction's summary outlives the turn that produced it. Every compacting path - plain chat, the agent loop, and manual `/compact` - appends the rendered summary to the committed active context AND the live history, because the compaction has already dropped the summarized messages for good and the checkpoint is the summary's only durable carrier. It is never added to the projected source events (host-generated, no source event of its own). Request injection stays ephemeral: the loop's own `Messages` remain structural so planning, idempotency, BaseDigest, and checkpoint bytes are unaffected. | `TestAutoCompactionSummarySurvivesTheTurnBoundary`, `TestAgentLoopCompactionSummarySurvivesTheTurnBoundary`, `TestPlainChatInjectsSummaryIntoStreamRequest`, `TestManualCompactSummarySurvivesRestartWithoutFurtherTurns` | 2026-08-15 |
| INV-AG-40 | Safety | Host-derived summary evidence is DISTINCT. Items are content-free (role plus size bucket), so several dropped messages collapse to one string; the summary envelope validator refuses duplicates and every summary caller degrades silently on a build error, so duplicates disabled summarization entirely while turns reported success. `contextmgr.OmittedEvidence` is the single producer and owns the rule, and the MaxSummaryItems cap bounds distinct items. | `TestOmittedEvidenceIsUnique`, `TestOmittedEvidenceCapCountsDistinctItems`, `TestOmittedEvidenceCapsDistinctItems`, `TestBuildCompactSummaryRequestDedupesEvidence` | 2026-08-15 |
| INV-AG-41 | Safety | An automatic compaction announces itself on every surface a turn has. The turn's agent-event callback is published on the session for the turn's duration, so `emitContextCompaction` reaches it from BOTH the agent loop and the plain (`--no-tools`) path - a headless `--json` consumer gets a typed `compaction` line for a threshold compaction, not only for `/compact`. | `TestPlainCompactionEmitsTypedEventToTheTurnCallback`, `TestCompactionEventReachesBridgeAsBanner`, `TestSlashCompactJSONEmitsTypedEvents` | 2026-08-15 |

## Security / Privacy

| ID | Category | Invariant | Test(s) | Last Verified |
|----|----------|-----------|---------|---------------|
| INV-SEC-1 | Safety | Local search skips secret paths **when `[tools].secret_path_patterns` are configured**. Nothing is compiled in, so an unconfigured workspace skips nothing (plan 04 §5) | `TestGrepNestedAndGlob`, `TestIsSecretPath`, `TestBlockEnvRead`, `TestIsSecretPathUnconfiguredFiltersNothing` | 2026-07-30 (restated for the config-only default) |
| INV-SEC-2 | Safety | Redaction is off by default everywhere, not only for tool args: with no `[privacy]` patterns configured, tool previews, event bodies and audit metadata are unredacted. Fail-open is deliberate (plan 10 §5) | `TestPrivacyRedactToolArgsDefaultOff` || 2026-07-30 (plan 10 implemented; no compiled patterns remain) |
| INV-SEC-3 | Safety | A z.ai error never carries the provider's own message: z.ai echoes request content back in it - prompt text, and the API key on some auth failures - and runtime redaction is off unless the workspace configures it, so nothing downstream would scrub it. What is reported instead is the numeric code plus a static explanation owned by this repo (`zaiCodeMeanings`). The code must survive every wire shape the service uses - flat `{"code":<int>}`, flat with a string code, and nested `{"error":{"code":"<string>"}}` - because it is then the *only* diagnostic a caller gets, and a shape mismatch silently degrades every failure to a bare HTTP status. The parser also runs on every SSE chunk at HTTP 200, so a code alone never declares failure: a failing status or an error field must accompany it | `TestZAIErrorParserNeverEchoesProviderMessage`, `TestZAIErrorParserExtractsCodeFromEveryWireShape`, `TestZAIErrorParserExplainsKnownCodes`, `TestZAIErrorParserPassesCleanPayloads`, `TestZAIErrorPathsDoNotEchoSecretsOrPrompt` | 2026-07-31 |
| INV-SEC-4 | Safety | **The durable context redaction policy is wired, and it redacts rather than refuses.** `SetContextRedactionPolicy` had no production caller, so a workspace's `[privacy]` rules classified tool previews, event bodies and audit metadata while durable context payloads went unclassified. It is now installed from the same compiled `redact.Policy` the rest of the process uses, passed as a `Redactor` function rather than re-implemented, because four hand-rolled pattern lists drifting apart is a failure this repo already paid for. Classification of a SOURCE PAYLOAD never returns an error: publication is one transaction, so refusing destroyed a turn the agent had already finished and wedged the session (INV-AG-35). A flagged payload is redacted through the host redactor and RE-CLASSIFIED - a redactor that missed something is not trusted to have cleaned it - and anything still flagged, or flagged with no redactor configured, degrades to metadata-only, which stores nothing and so leaks nothing while the reference still describes the real message. Fail-open survives (INV-SEC-2): a workspace configuring no patterns yields the zero policy, stores no bytes and classifies nothing, so enabling redaction is what starts persisting message content - a deliberate, documented consequence of asking for it. Summaries keep the OPPOSITE treatment on purpose: they are host-generated and regenerable, so refusing one keeps model output carrying a secret out of storage and off the wire at no cost to the user's conversation | `TestContextRedactionPolicyReachesTheProjector`, `TestContextRedactionPolicyUsesTheProcessRedactor`, `TestUnconfiguredWorkspaceKeepsMetadataOnlyPayloads`, `TestConfiguredSessionStoresRedactedPayloads`, `TestConfiguredRedactionRedactsInsteadOfRefusing`, `TestUnredactableSecretDegradesToMetadata`, `TestIneffectiveRedactorStillDegradesToMetadata`, `TestCleanPayloadUnderConfiguredPolicyIsStoredWhole`, `TestSummaryClassificationStillRefuses`, `TestContextCompactionInvariants` | 2026-08-02 |

| INV-TUI-28 | Safety | Dialog geometry and composition use one bounded cell layout: the modal is centered within the raw terminal, its rendered canvas is exact, resize reflows/clamps pager state, and ANSI styles are carried across both seams without leaking | `TestDialogRectFitsAndCenters`, `TestDialogViewsStayWithinTerminalBounds`, `TestDialogResizeRecentersAndReflows`, `TestDialogResizeClampsScroll`, `TestDialogCompositorExactCanvas`, `TestDialogANSISeamsPreserveStyles`, `TestDialogViewsStayWithinTerminalBoundsCompositor` | 2026-07-31 |
| INV-TUI-29 | Safety | Modal input owns every mouse and paste event while open; transcript copy, stale hit maps, composer focus, selection, and viewport fallback are unreachable, while key precedence remains global | `TestModalMouseEventsAreFullySwallowed`, `TestModalOpenSwallowsMouseBeforeView`, `TestModalOpenSwallowsMouseAfterView`, `TestModalCloseInvalidatesHitMapBeforeView`, `TestModalCloseRebuildsHitMapAfterView`, `TestModalKeyPrecedenceBeforeAndAfterView`, `TestAsyncPasteDroppedWhileModalOpens` | 2026-07-31 |
| INV-TUI-30 | Safety | Block/fleet detail remains complete and reachable through the shared near-full pager; the sessions sidebar keeps its selected row visible; status uses a compact non-paging overflow summary | `TestBlockOverlayPreservesLongLines`, `TestSessionsSidebarViewKeepsSelectedRowVisible`, `TestStatusDialogOverflowPolicy` | 2026-07-31 |
| INV-TUI-31 | Safety | Reference producers retain semantic content for current-width help rendering, identify captured snapshots, and survive the open/resize/wheel/close/post-modal-input journey | `TestHelpReflowsAfterResize`, `TestStatusAndFleetSnapshotPolicy`, `TestDialogProgramResizeIntegration` | 2026-07-31 |
| INV-TUI-32 | Safety | The model picker renders every explicit provider group, keeps slash-containing IDs and duplicate IDs distinct, marks the full provider/model selection, disables unavailable rows safely, and commits only an enabled row through Enter using the same session binding factory as direct switches | `TestIntegrationModelDialogShowsCatalogAndCommitsSelection`, `TestIntegrationModelDialogDisabledRowCannotCommit`, `TestIntegrationModelDialogCommitsEnabledCrossProviderSelection`, `TestIntegrationModelDialogUsesSessionBindingFactory`, `TestIntegrationModelDialogDirectCommandUsesCurrentProvider`, `TestIntegrationModelDialogStaysWithinTinyCanvases` | 2026-07-31 |

## Agent messaging invariants

| ID | Category | Invariant | Test(s) | Last Verified |
|----|----------|-----------|---------|---------------|
| INV-MSG-1 | Safety | Early fence declines an ask when the responder handler is already done — the ask never reaches a dead handler | `TestEarlyFenceDeclinesAskWhenResponderHandlerDone` | |
| INV-MSG-2 | Safety | Pool OnTaskDone receives stamped context and result; a nil pool callback is a safe no-op | `TestPoolOnTaskDoneReceivesStampedContextAndResult`, `TestPoolNilOnTaskDoneIsNoOp` | |
| INV-MSG-3 | Safety | Cancel-without-retry preserves the genuine failure reason, preserves genuine timeout, and transitions a running task to canceled — none is masked by retry | `TestCancelNoRetryKeepsGenuineFailure`, `TestCancelNoRetryKeepsGenuineTimeout`, `TestCancelNoRetryRunningTaskBecomesCanceled` | |
| INV-MSG-4 | Safety | Mark-canceled without results is recorded in the ledger — the cancellation is not silently lost | `TestMarkCanceledWithoutResultsLedgerAware` | |
| INV-MSG-5 | Safety | Ask delivery is declined when the target has already completed; an ask is delivered only before the target completes; delivery is declined to terminal mailboxes; and sealing an open ask refuses a claimed ask | `TestAskDeclinedWhenTargetCompletes`, `TestAskDeliveredBeforeTargetCompletes`, `TestMailboxSendDeclinesAskDeliveredToTerminalMailbox`, `TestMailboxSendDeclinesAskRacingTerminal`, `TestSealOpenAskAnswerRefusesClaimedAsk` | |
| INV-MSG-6 | Safety | Retry resets the ask quota; a NUL answer payload is rejected and never reaches the parked asker | `TestRetryResetsAskQuota`, `TestNULAnswerRejectedNeverReachesParkedAsker` | |
| INV-MSG-7 | Safety | Parked questions respect max-wait TTL — a max-wait longer than TTL is not prematurely evicted; an expired max-wait evicts; an expired park does not block a new park; and delivering an answer to an expired park returns false | `TestParkQuestionMaxWaitLongerThanTTLNotEvicted`, `TestParkQuestionMaxWaitExpiredEvicts`, `TestParkQuestionExpiredDoesNotBlockNewPark`, `TestDeliverAnswerExpiredParkReturnsFalse` | |
| INV-MSG-8 | Safety | End-to-end ask/answer round trip completes through dispatch; skill surface injection cannot resurrect a disallowed post_message tool | `TestE2E_DispatchAskAnswerRoundTrip`, `TestSkillSurfaceInjectionDoesNotResurrectDisallowedPostMessage` | |
| INV-MSG-9 | Safety | Subagent protocol prompt teaches all four post_message kinds and their required fields (finding, question, ask, answer; in_reply_to, to_role, wait_seconds) | `TestMessagingProtocolPromptTeachesPostMessageKinds` | |
| INV-MSG-10 | Safety | Subagent protocol prompt never mentions parent-only tools (run_messages, send_to_task) that sub-agents must not invoke | `TestMessagingProtocolPromptDoesNotTeachParentTools` | |
| INV-MSG-11 | Safety | Subagent protocol prompt wraps parent messages in a structural delimiter tag and explicitly forbids treating them as instructions | `TestMessagingProtocolPromptAntiInjection` | |
| INV-MSG-12 | Safety | Subagent protocol prompt omits the word "handler" (case-insensitive) so the model cannot infer internal dispatch terminology | `TestMessagingProtocolPromptOmitsHandlerWord` | |
| INV-MSG-13 | Safety | Subagent protocol prompt mentions ask caps and boundedness, and states that exceeded asks are declined immediately | `TestMessagingProtocolPromptMentionsCapsAndBoundedness` | |
| INV-MSG-14 | Safety | Subagent protocol prompt stays under 1200 bytes so it does not consume disproportionate token budget in nested turns | `TestMessagingProtocolPromptKeepsLengthReasonable` | |
| INV-MSG-15 | Safety | Routed agent prompt includes the messaging protocol block so delegated agents can communicate with siblings | `TestRoutedAgentPromptIncludesProtocol` | |
| INV-MSG-16 | Safety | Skill agent prompt includes the messaging protocol block so skill-bound agents inherit inter-agent communication | `TestSkillAgentPromptIncludesProtocol` | |
| INV-MSG-17 | Safety | Plain multi-step prompt includes the messaging protocol block so the default multi-step path carries the protocol | `TestPlainMultiStepPromptIncludesProtocol` | |
| INV-MSG-18 | Safety | One-shot delegate prompts do not include the messaging protocol block since one-shot agents have no ask/answer lifecycle | `TestOneshotDelegateDoNotIncludeProtocol` | |
| INV-MSG-19 | Safety | Root prompts teach parent-level messaging so the top-level agent can post_message to child tasks | `TestRootPromptsTeachParentMessaging` | |
| INV-MSG-20 | Safety | Root prompt messaging block omits the word "handler" consistent with the subagent protocol prompt | `TestRootPromptMessagingBlockOmitsHandler` | |
| INV-MSG-21 | Safety | Protocol markers agree across all prompt surfaces — the same structural tags and vocabulary are used everywhere the protocol is taught | `TestProtocolMarkersAgreeAcrossSurfaces` | |

## Admission invariants

| ID | Category | Invariant | Test(s) | Last Verified |
|----|----------|-----------|---------|---------------|
| INV-ADM-1 | Safety | Stage admission enforces a name cap and refuses over-limit requests | `TestStageAdmissionNameCapRefusesOverLimit` | |
| INV-ADM-2 | Safety | Name cap excludes already-staged re-requests from the count | `TestNameCapExcludesAlreadyStagedReRequests` | |
| INV-ADM-3 | Safety | Name cap counts the total across both published and pending stages | `TestNameCapCountsTotalAcrossPublishedAndPending` | |
| INV-ADM-4 | Safety | Name cap allows a fresh stage after a reset | `TestNameCapAllowsFreshStageAfterReset` | |
| INV-ADM-5 | Safety | Perpetual deferral accumulation is capped to prevent unbounded growth | `TestPerpetualDeferralAccumulationCapped` | |
| INV-ADM-6 | Safety | A configured core tier never defers an authorized MCP tool, even though the tier list cannot name the tool's runtime-hashed name (`DC-15`) | `TestPlanToolTiersNeverDefersAnAuthorizedMCPTool`, `TestWithMCPServerToolsAlwaysCoreDoesNotMutateTheConfiguredCoreSlice`, `TestRunConfiguredChatKeepsGlobalMCPToolInCoreTierEvenWithAConfiguredCoreList` | 2026-08-09 |

## Durable execution invariants

These two rows pin the classes with the worst repeat-fix chains in this repository:
`DC-1` and `DC-2` in `.mivia/quality/defect-taxonomy.md`. Both are invisible to the
race detector, because the interleaving happens between transactions and the writers
may be separate processes or hosts.

| ID | Category | Invariant | Test(s) | Last Verified |
|----|----------|-----------|---------|---------------|
| INV-DUR-1 | Safety | Every terminal run state has an enumerated re-entry rule. A transient condition never reaches a terminal state without one: a forward-advanced base is a normal condition that still delivers, a rewritten or deleted base is the only permanent refusal, and `delivery_failed` is re-enterable through a single enforcing CAS while the terminal invariant holds | `TestDeliverRemoteBaseForwardAdvanced`, `TestDeliverRemoteBaseRewrittenRefused`, `TestDeliverDeliveryFailedReentry`, `TestWorkflowDeliverReopensDeliveryFailed` | 2026-08-08 (registered from the `b977729` fix chain) |
| INV-DUR-2 | Safety | A durable owner's write is fenced. After a takeover the previous owner's next mutation fails instead of winning, a claim is exclusive across repositories and processes, and one invocation key admits exactly one run | `TestStorageRepository_TakeoverFencesBoundOldWriter`, `TestIntegrationClaimFencesWorkflowMutation`, `TestWorkflowDeliverClaimFencing`, `TestMemoryBackendClaimIsExclusive`, `TestResumeRefusesRunHeldByAnotherExecutor`, `TestInvocationKeyAdmitsOneRunAcrossSQLiteRepositories` | 2026-08-08 (registered from the claim/fence fix chain) |

## Storage path invariants

| ID | Category | Invariant | Test(s) | Last Verified |
|----|----------|-----------|---------|---------------|
| INV-STORE-1 | Safety | A bare relative store path (no directory separator) opens as a database file in the current directory, never as a directory (DC-10): `OpenSQLite` must not `MkdirAll` a directory named like the DB file, which made the WAL PRAGMA fail and left a stray directory - with `[subagents] store_path` set to a bare name the ledger then silently fell back to the memory backend and the context store failed to open. Empty and whitespace-only paths are refused before any filesystem work; nested-relative and absolute paths keep their existing behavior | `TestOpenSQLiteBareFilenameOpensDatabaseFile`, `TestOpenSQLiteRejectsEmptyPath`, `TestOpenSQLitePathShapeBoundary` | |

## Workflow observability invariants

| ID | Category | Invariant | Test(s) | Last Verified |
|----|----------|-----------|---------|---------------|
| INV-WF-1 | Safety | Every workflow step attempt and every run transition is observable: a durable `wf_*` ledger event is written for run created/status/attempt started/completed/execution/resumed, and the controller emits a progress event at step start/end and run finish when a `ProgressSink` is attached | `TestLinearProgressStepStartedAndCompletedOnSuccess`, `TestPanelStepFailureCausePersistsDurableCause`, `TestControllerRunEmitsRunFinished`, `TestStartNewResumeRecordsRunResumed` | |

## Prefix-stability invariants (plan 68)

| ID | Category | Invariant | Test(s) | Last Verified |
|----|----------|-----------|---------|---------------|
| INV-68-1 | Safety | PrefixIdentity equality is necessary and sufficient for byte-equal request prefixes: no wire-affecting input (provider/model, reasoning dial, temperature value pair, tool-schema digest, system-prompt digest) may be absent from the identity | `TestPrefixIdentityStableAcrossEqualBindingCaptures`, `TestChatTurnRequestBodyIsByteStableAcrossEqualPrefixIdentity` | |
| INV-68-2 | Safety | Every wire-affecting identity field change emits exactly one KindPrefixReset naming the changed category(ies); equal identity and refused paths emit nothing (no false reset) | `TestSwitchBindingEmitsPrefixResetNamingChangedCategory`, `TestPublishAgentSurfaceEmitsPrefixResetOnAgentSwitch`, `TestPublishAgentSurfaceEmitsPrefixResetOnToolAdmission` | |
| INV-68-3 | Safety | Equal identity plus equal message history produces byte-identical serialized request bodies | `TestChatTurnRequestBodyIsByteStableAcrossEqualPrefixIdentity` | |
| INV-68-4 | Safety | PrefixIdentity and KindPrefixReset are read-only observability, never consulted by runtime.Dispatcher, ScopedRegistry, or any authorization check | `TestPrefixResetAuthorityUnchanged` | |
| INV-68-5 | Safety | The deferred-tool index is frozen within one AgentSurfaceGeneration and never re-rendered after an admission | `TestDeferredIndexUnchangedAcrossAdmissionsWithinOneGeneration` | |
| INV-68-6 | Safety | The evidenceRefsBlock fixed header is a byte-identical prefix of every step prompt in a run | `TestRenderStepPromptEvidenceHeaderIsByteIdenticalAcrossSteps` | |
| INV-68-7 | Safety | KindPrefixReset telemetry carries no prompt content, digest preimage, tool-schema body, or tool-argument value | `TestPrefixResetEventIsSealedAndContentFree` | |
| INV-68-8 | Safety | Prefix identity capture stays off the per-turn hot path: only NewSession, SwitchBinding, TryPublishAgentSurface, and SetReasoningEffort capture | `TestPrefixIdentityNotCapturedOnPerTurnSavePath` | |

## Liveness Gap Notes

| ID | Gap | Mitigation | Feasibility |
|----|-----|------------|-------------|
| INV-TUI-2 | Unit tests verify the pollCmd returns a command but cannot prove the chain never starves under real I/O scheduling | **Phase 3:** Added `TestTuiTickMsgStressRequeuesPoll` (500 rapid ticks, each verifies non-nil cmd) + `TestTuiTickMsgStressWithBridgeData` (100 ticks with concurrent bridge writes). Full integration with simulated slow bridge + real TTY requires bubbletea test framework (deferred). | **Stress test: feasible.** Integration: deferred. Residual risk: kernel scheduler edge cases require production monitoring. |
| INV-TUI-6 | Unit test verifies events are published but cannot prove the TUI paints them within 100ms under parallel dispatch | **Phase 3:** Added concurrent stress tests (`TestStreamBridgeConcurrentDispatchCompleteness`, `TestStreamBridgeConcurrentDispatchAndTUIApply`, `TestBridgeConcurrentWriteAndDrainRace`, `TestBridgeConcurrentFinishAndDrainRace`, `TestStreamBridgeConcurrentActiveToolsNoDeadlock`). These verify event completeness, TUI apply without deadlock, and concurrent bridge safety. A visual/acceptance regression suite is deferred. | **Stress test: feasible.** Visual timing: deferred (requires acceptance framework). |

## Maintenance

- **Test renames:** If a test listed above is renamed or moved, update this manifest and ensure the renamed test still verifies the same invariant.
- **Validation:** Run `make validate-invariants` before committing manifest changes to confirm all referenced tests exist in the codebase.
- **Additions:** When adding a new invariant, include the test name(s) and category. Prefer Safety over Liveness where possible.
