#!/usr/bin/env bash
# Wrapper: offline secret scan via scripts/secret_scan.py (no network).
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$ROOT"

if [[ $# -eq 0 ]]; then
  exec python3 scripts/secret_scan.py --tracked
fi
exec python3 scripts/secret_scan.py "$@"
