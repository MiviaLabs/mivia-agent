#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
TIMEOUT="${MIVIA_PRE_COMMIT_TIMEOUT_SECONDS:-60}"
exec "$ROOT/scripts/git-hooks/run_with_timeout" "$TIMEOUT" "$ROOT/scripts/git-hooks/pre-commit" "$@"
